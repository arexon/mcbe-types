{
  "workspace": ["./packages/*"],
  "tasks": {
    "test": "deno test --allow-all --parallel --doc",
    "ready": "deno task test && deno fmt --check && deno lint"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.16",
    "@std/text": "jsr:@std/text@^1.0.16"
  }
}
